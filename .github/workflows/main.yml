name: restartServer
on: [push]
jobs:
 check-bats-version:
   runs-on: ubuntu-latest
   steps:
     - run: |
         curl https://raw.githubusercontent.com/huang-feiyu/academics/main/README.md --output README.md.tmp,
         mv README.md.tmp README.md.
